<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ListView xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent" android:layout_height="match_parent" android:background="@color/white" android:id="@+id/fitness_list" android:paddingTop="40sp"
        android:divider="@android:color/transparent"
        android:dividerHeight="5.0sp">
    </ListView>

    <ImageButton
        android:id="@+id/runCommand"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="end|bottom"
        android:layout_marginBottom="16dp"
        android:layout_marginEnd="16dp"
        android:tint="@android:color/white"
        android:background="@drawable/oval"
        android:src="@android:drawable/ic_input_add"
        android:elevation="1dp"
        android:stateListAnimator="@anim/button_elevation"
        tools:ignore="RtlCompat"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monday 3rd April"
        android:textSize="20sp"
        android:id="@+id/dateText"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="5sp" />
</FrameLayout>